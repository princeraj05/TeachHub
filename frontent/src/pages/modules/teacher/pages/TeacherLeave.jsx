import { useEffect, useState } from "react"; import axios from "axios";
export default function TeacherLeave(){const API=import.meta.env.VITE_API_URL, headers={Authorization:`Bearer ${localStorage.getItem("token")}`};const [leaves,setLeaves]=useState([]),[message,setMessage]=useState("");const load=()=>axios.get(`${API}/api/teacher-leaves`,{headers}).then(r=>setLeaves(r.data));useEffect(load,[]);const submit=async e=>{e.preventDefault();const f=new FormData(e.target);try{await axios.post(`${API}/api/teacher-leaves`,Object.fromEntries(f),{headers});e.target.reset();setMessage("Leave request submitted.");load()}catch(err){setMessage(err.response?.data?.message||"Could not submit leave")}};return <div className="max-w-3xl mx-auto space-y-6"><h1 className="text-2xl font-extrabold dark:text-white">On Leave</h1><form onSubmit={submit} className="grid sm:grid-cols-2 gap-3 rounded-2xl bg-white dark:bg-[#0B132A] border p-5"><input name="startDate" type="date" required/><input name="endDate" type="date"/><textarea className="sm:col-span-2" name="reason" placeholder="Reason (optional)"/><button className="sm:col-span-2 py-3 rounded-xl bg-[#7C3AED] text-white font-bold">Submit leave request</button></form>{message&&<p className="text-sm text-violet-700">{message}</p>}<div className="space-y-2">{leaves.map(x=><div key={x._id} className="rounded-xl bg-white dark:bg-[#0B132A] border p-4 text-sm dark:text-white flex justify-between"><span>{new Date(x.startDate).toLocaleDateString()} – {new Date(x.endDate).toLocaleDateString()}</span><b>{x.status}</b></div>)}</div></div>}
