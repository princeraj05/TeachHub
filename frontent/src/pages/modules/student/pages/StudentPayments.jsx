import { useCallback, useEffect, useState } from "react";
import axios from "axios";
import { FaCreditCard, FaHistory, FaUniversity } from "react-icons/fa";
import { startBackendPayment } from "../../../../utils/razorpayCheckout";

const money = value => new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR" }).format((value || 0) / 100);

export default function StudentPayments() {
  const api = import.meta.env.VITE_API_URL;
  const token = localStorage.getItem("token");
  const [payments, setPayments] = useState([]); const [plan, setPlan] = useState(null); const [summary, setSummary] = useState(null); const [options, setOptions] = useState(null); const [busy, setBusy] = useState(false); const [message, setMessage] = useState("");
  const headers = { Authorization: `Bearer ${token}` };
  const load = useCallback(async () => { try { const [history, fee, modes, paymentSummary] = await Promise.all([axios.get(`${api}/api/payments`, { headers }), axios.get(`${api}/api/student-payments/fee-plan`, { headers }), axios.get(`${api}/api/payment-options`, { headers }), axios.get(`${api}/api/student-payments/summary`, { headers })]); setPayments(history.data); setPlan(fee.data); setOptions(modes.data); setSummary(paymentSummary.data); } catch { setMessage("Payment information is currently unavailable."); } }, [api, token]);
  useEffect(() => { load(); }, [load]);
  const payOnline = async () => { setBusy(true); setMessage(""); try { const result = await startBackendPayment({ apiBase: api, token, createOrderEndpoint: "/api/student-payments/create-order", customer: { name: localStorage.getItem("name") } }); setMessage(result.status === "Successful" ? "Payment verified successfully." : "Payment is being verified."); await load(); } catch (e) { try { await axios.post(`${api}/api/student-payments/cancel-processing`, {}, { headers }); } catch (err) { console.error("Failed to cancel processing payment:", err.message); } setMessage(e.response?.data?.message || e.message || "Payment could not be completed."); await load(); } finally { setBusy(false); } };
  const requestOffline = async () => { const reference = window.prompt("Enter your bank/UPI/cheque reference"); if (!reference) return; const method = window.prompt("Enter CASH, BANK_TRANSFER, MANUAL_UPI, or CHEQUE", "MANUAL_UPI"); if (!method) return; setBusy(true); try { await axios.post(`${api}/api/student-payments/offline-request`, { reference, method }, { headers }); setMessage("Offline payment request submitted for verification."); await load(); } catch (e) { setMessage(e.response?.data?.message || "Offline request could not be submitted."); } finally { setBusy(false); } };
  const getReceipt = async (id, download = false) => {
    try {
      const response = await axios.get(`${api}/api/payments/${id}/receipt?download=1`, {
        headers,
        responseType: "blob"
      });
      const url = URL.createObjectURL(response.data);
      if (!download) {
        window.open(url, "_blank");
      } else {
        const link = document.createElement("a");
        link.href = url;
        link.download = `receipt-${id}.html`;
        link.click();
      }
    } catch {
      setMessage("Receipt is not available.");
    }
  };
  const locked = !summary?.paymentAvailable;
  return <div className="max-w-5xl mx-auto space-y-6"><div><p className="text-[10px] font-bold uppercase tracking-widest text-[#7C3AED]">Secure payments</p><h1 className="text-2xl font-extrabold text-slate-800 dark:text-white">Pay School Fee</h1><p className="text-sm text-slate-500 mt-1">Amounts and payment status are verified by TeachHub.</p></div>{message && <div className="rounded-xl border border-indigo-200 bg-indigo-50 px-4 py-3 text-sm text-indigo-800">{message}</div>}<div className="grid grid-cols-2 sm:grid-cols-4 gap-3"><Stat label="Current fee" value={money(summary?.currentFee)} /><Stat label="Payment status" value={summary?.paymentStatus || "Pending"} /><Stat label="Due date" value={summary?.dueDate ? new Date(summary.dueDate).toLocaleDateString() : "—"} /><Stat label="Validity Remaining" value={summary?.remainingDays !== undefined ? `${summary.remainingDays} Days` : "—"} /></div><section className="rounded-2xl bg-white dark:bg-[#0B132A] border border-slate-200 dark:border-white/10 p-6 shadow-sm flex flex-col sm:flex-row justify-between gap-5"><div><div className="flex items-center gap-2 text-[#7C3AED] font-bold"><FaCreditCard /> Current school fee</div><p className="mt-3 text-3xl font-extrabold text-slate-800 dark:text-white">{plan ? money(plan.monthlyFee) : "Not configured"}</p><p className="mt-1 text-xs text-slate-500">One verified payment is permitted per billing month. The final amount always comes from the secure server.</p></div><div className="flex flex-col sm:w-52 gap-2">{options?.available === false ? <p className="rounded-xl bg-amber-50 p-3 text-sm font-bold text-amber-800">Payment is currently unavailable. Please contact your administrator.</p> : locked ? <p className="rounded-xl bg-emerald-50 p-3 text-sm font-bold text-emerald-800">{summary?.paymentStatus === "PendingVerification" ? "This billing month is awaiting verification." : "This billing month is already paid."}</p> : <>{options?.onlineEnabled && <button disabled={busy || !plan?.active} onClick={payOnline} className="rounded-xl bg-[#7C3AED] px-4 py-3 text-sm font-bold text-white disabled:opacity-50">{busy ? "Please wait…" : "Pay online"}</button>}{options?.offlineEnabled && <div className="rounded-xl bg-purple-50 dark:bg-purple-950/20 border border-purple-200 dark:border-purple-900/50 p-3 text-xs font-semibold text-purple-900 dark:text-purple-300 leading-normal">For offline payment: Meet your principal and do payment</div>}</>}</div></section><section className="rounded-2xl bg-white dark:bg-[#0B132A] border border-slate-200 dark:border-white/10 overflow-hidden"><div className="p-5 border-b border-slate-100 dark:border-white/10 font-bold text-slate-800 dark:text-white"><FaHistory className="inline mr-2 text-[#7C3AED]" />Payment history & receipts</div><div className="divide-y divide-slate-100 dark:divide-white/10">{payments.length ? payments.map(p => <div key={p._id} className="p-4 flex flex-wrap items-center justify-between gap-2 text-sm"><div><p className="font-bold text-slate-800 dark:text-white">{money(p.amount)} · {p.purpose.replaceAll("_", " ")}</p><p className="text-xs text-slate-500">{new Date(p.createdAt).toLocaleDateString()} {p.receiptNumber ? ` · Receipt ${p.receiptNumber}` : ""}</p></div><div className="flex gap-2 items-center"><span className="rounded-full bg-slate-100 dark:bg-white/10 px-3 py-1 text-xs font-bold text-slate-600 dark:text-slate-300">{p.status}</span>{["Pending", "Processing"].includes(p.status) && p.gateway === "razorpay" && <button onClick={async () => { setMessage("Checking payment status with Razorpay..."); try { const { data } = await axios.post(`${api}/api/payments/${p._id}/verify-status`, {}, { headers }); if (data.status === "Successful") { setMessage("Payment verified successfully."); } else { setMessage(data.message || "Payment is still processing or failed."); } load(); } catch (err) { setMessage(err.response?.data?.message || "Failed to verify payment status."); } }} className="rounded-lg border border-indigo-300 px-2 py-1 text-xs font-bold text-indigo-655 hover:bg-indigo-50">Verify</button>}{p.receiptNumber && <><button onClick={() => getReceipt(p._id, false)} className="rounded-lg border px-2 py-1 text-xs font-bold">View</button><button onClick={() => getReceipt(p._id, true)} className="rounded-lg border px-2 py-1 text-xs font-bold">Download</button></>}</div></div>) : <p className="p-6 text-sm text-slate-500">No payments yet.</p>}</div></section></div>;
}
function Stat({ label, value }) { return <div className="rounded-xl bg-white dark:bg-[#0B132A] border p-4 text-sm"><b>{label}</b><p className="text-lg font-extrabold mt-1">{value}</p></div>; }
