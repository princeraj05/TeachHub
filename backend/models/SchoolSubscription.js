const mongoose = require("mongoose");
const schema = new mongoose.Schema({ schoolName: { type: String, unique: true, required: true, index: true }, monthlyFee: { type: Number, required: true, min: 1 }, currency: { type: String, default: "INR" }, billingStartDate: { type: Date, default: null }, nextBillingDate: { type: Date, default: null }, currentBillingPeriod: { start: Date, end: Date }, gracePeriodDays: { type: Number, default: 0, min: 0, max: 90 }, status: { type: String, enum: ["Active", "Due Soon", "Payment Due", "Grace Period", "Free", "Suspended", "Cancelled"], default: "Payment Due" }, updatedBy: { type: mongoose.Schema.Types.ObjectId, ref: "User", required: true } }, { timestamps: true });
module.exports = mongoose.model("SchoolSubscription", schema);
